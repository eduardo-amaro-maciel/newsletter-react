import './Nav.css'

export default function Nav() {

    return (
        <nav className="nav-top">
            <button className="btn" id="btn-sign-in">Sign in</button>
            <button className="btn" id="btn-sign-up">Sign up</button>
        </nav>
    )
}